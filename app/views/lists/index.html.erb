<h1>Lists</h1>

    <% @lists.each do |list| %>
      <div><%= link_to list.name, list_path(list) %> | <%= link_to "🗑️", list_path(list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></div>
    <% end %>
  <%= link_to "New list", new_list_path, class:"btn btn-secondary"%></div>
